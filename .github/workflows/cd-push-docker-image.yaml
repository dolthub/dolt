name: Push Docker Image to DockerHub

#on:
#  workflow_dispatch:
#    inputs:
#      version:
#        description: 'SemVer format release tag, i.e. 0.24.5'
#        required: true
#  repository_dispatch:
#    types: [ push-docker-image ]
on:
  pull_request:
    branches: [ main ]

jobs:
  docker-image-push:
    name: Push Docker Image
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        with:
          platforms: linux/amd64,linux/arm64
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./docker/Dockerfile
          push: true
          tags: dolthub/dolt-image:${{ github.event.inputs.version || github.event.client_payload.version }} , dolthub/dolt-image:latest
          build-args: |
#            DOLT_VERSION=${{ github.event.inputs.version || github.event.client_payload.version }}
            DOLT_VERSION=0.50.8