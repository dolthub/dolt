CREATE TABLE employees (
    `id` LONGTEXT NOT NULL,
    `first name` LONGTEXT,
    `last name` LONGTEXT,
    `title` LONGTEXT,
    `start date` LONGTEXT,
    `end date` LONGTEXT,
    PRIMARY KEY (`id`)
);