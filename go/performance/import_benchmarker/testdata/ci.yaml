opts:
  seed: 0
tests:
- name: "row count"
  repos: &repos
  - name: dolt_cli
  - name: dolt_server
    server:
      port: 3308
      args: [ "--port", "3308" ]
  - name: mysql
    external-server:
      name: test
      host: 127.0.0.1
      user: root
      password: password
      port: 3309
  tables:
    - name: "400k"
      fmt: "csv"
      rows: 400000
      schema: |
        create table xy (
          x int primary key,
          y varchar(30)
        );