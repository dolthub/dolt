// serialization format prototype using flatbuffers

include "common.fbs";

namespace serial;

table Root {
    // todo(andy): can datasets be inline, or
    //  do we need a prolly tree?
    datasets:RefMap (required);
}

table Database {
  // todo(andy): can tables be inline, or
  //  do we need a prolly tree?
  tables:RefMap (required);

  // todo(andy): foreign keys
}

table Table {
  filler:bool;
}
