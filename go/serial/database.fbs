// serialization format prototype using flatbuffers

include "common.fbs";
include "prolly.fbs";
include "schema.fbs";


namespace serial;

table Root {
    datasets:ProllyNode (required);
}

table DatabaseRoot {
  tables:ProllyNode (required);
  foreign_keys:[ForeignKey] (required);
}

table Table {
  filler:bool;
}
