CREATE TABLE employees (
    `id` varchar(20) NOT NULL,
    `first name` LONGTEXT,
    `last name` LONGTEXT,
    `title` LONGTEXT,
    `start date` LONGTEXT,
    `end date` LONGTEXT,
    PRIMARY KEY (`id`)
);
