CREATE TABLE `newtable`(
    `pk` BIGINT NOT NULL,
    `int` BIGINT,
    `string` LONGTEXT,
    `boolean` BOOLEAN,
    `float` DOUBLE,
    `uint` BIGINT UNSIGNED,
    `uuid` LONGTEXT,
    PRIMARY KEY (`pk`)
);
