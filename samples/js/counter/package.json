{
  "name": "@attic/counter",
  "private": true,
  "description": "noms example javascript client with a counter",
  "license": "Apache-2.0",
  "main": "dist/main.js",
  "version": "1.0.1",
  "scripts": {
    "preinstall": "ln -sf ../../../js/noms/.babelrc && ln -sf ../.eslintrc.js && ln -sf ../../../js/noms/.flowconfig",
    "prepublish": "yarn run build",
    "start": "babel -d dist -w src",
    "build": "BABEL_ENV=production babel -d dist src",
    "test": "eslint src/ && flow src/"
  },
  "dependencies": {
    "@attic/noms": "file:../../../js/noms",
    "babel-cli": "^6.18.0",
    "babel-preset-noms": "1.0.1",
    "eslint": "^3.13.1",
    "eslint-config-noms": "1.0.0",
    "flow-bin": "^0.36.0",
    "yargs": "^6.6.0"
  },
  "keywords": [
    "noms",
    "sample"
  ]
}
