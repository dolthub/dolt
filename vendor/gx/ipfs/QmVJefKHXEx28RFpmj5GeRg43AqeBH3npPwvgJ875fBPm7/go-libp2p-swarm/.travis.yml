sudo: false

os:
  - linux

language: go

go:
    - 1.8

install: true

before_install:
  - make deps

script:
  - go vet
  - $GOPATH/bin/goveralls -service="travis-ci"

cache:
    directories:
        - $GOPATH/src/gx

notifications:
  email: false
